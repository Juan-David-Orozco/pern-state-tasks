import {Button} from "@mui/material"

export default function Navbar() {
  return (
    <div>
      Navbar
      <Button
        variant="contained"
        color="primary"
      >
        Click Me
      </Button>
    </div>
  )
}
